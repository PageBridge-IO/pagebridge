{
  "name": "gsc-sanity-connector",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "pnpm --filter @gsc-sanity/db db:generate",
    "db:migrate": "pnpm --filter @gsc-sanity/db db:migrate",
    "db:push": "pnpm --filter @gsc-sanity/db db:push",
    "db:studio": "pnpm --filter @gsc-sanity/db db:studio",
    "sync": "pnpm --filter @gsc-sanity/cli start sync"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.8.2",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
